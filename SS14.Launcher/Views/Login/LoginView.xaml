<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vml="clr-namespace:SS14.Launcher.ViewModels.Login;assembly=SS14.Launcher"
             xmlns:loc="clr-namespace:SS14.Launcher.Localization"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="SS14.Launcher.Views.Login.LoginView"
             Name="Login">
  <Design.DataContext>
    <vml:LoginViewModel />
  </Design.DataContext>

  <DockPanel LastChildFill="False">
    <TextBlock HorizontalAlignment="Center" DockPanel.Dock="Top" Classes="NanoHeadingMedium" 
               Text="Sandwich Unified ID Login" />

    <!-- Instructions -->
    <TextBlock HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0, 10, 0, 0" TextWrapping="Wrap"
               MaxWidth="400" Text="Link your SS14 account with your Discord account to continue." />

    <!-- Open Web Dashboard Button -->
    <Button DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0, 15, 0, 0" MinWidth="200"
            Content="Open Web Dashboard"
            Command="{Binding OpenWebLinker}"
            IsEnabled="{Binding !Busy}" />

    <!-- Divider -->
    <TextBlock HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0, 15, 0, 10" Text="or enter verification code:" />

    <!-- Verification Code Input -->
    <TextBox DockPanel.Dock="Top" Name="CodeBox" MaxWidth="300" Margin="0, 4, 0, 0"
             Watermark="Enter verification code"
             Text="{Binding VerificationCode, Mode=TwoWay}" 
             IsEnabled="{Binding !Busy}" />

    <!-- Link Account Button -->
    <DockPanel DockPanel.Dock="Top" LastChildFill="False" MaxWidth="300" Margin="0 15 0 0">
      <Button DockPanel.Dock="Right" HorizontalAlignment="Center"
              Content="Link Account"
              Command="{Binding OnLinkButtonPressed}"
              IsEnabled="{Binding IsCodeValid}">
      </Button>
    </DockPanel>

    <!-- Status Messages -->
    <Border DockPanel.Dock="Top" Margin="0, 15, 0, 0" MaxWidth="400" Padding="10" CornerRadius="4"
            Background="{DynamicResource SystemErrorColor}" IsVisible="{Binding ShowStatusError}">
      <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="White" />
    </Border>

    <Border DockPanel.Dock="Top" Margin="0, 15, 0, 0" MaxWidth="400" Padding="10" CornerRadius="4"
            Background="{DynamicResource SystemSuccessColor}" IsVisible="{Binding !ShowStatusError, Converter={x:Static ObjectConverters.IsNotNull}}">
      <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="White" 
                 IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
    </Border>
  </DockPanel>
</UserControl>
